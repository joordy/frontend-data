// export function createHTML(chartType) {
//   const elem = document.createElement('section');
//   const classElem = document.createAttribute('class').value(chartType);

//   elem.appendChild(classElem);

//   document.getElementsByClassName('charts').appendChild(elem);
// }
